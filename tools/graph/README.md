# Graph 

dependency graph visualizer for repomix outputs.
parses the `repomix-output.xml` file and builds an interactive graph using Cytoscape.js.

## usage

to run locally:

```bash
cd tools/graph
npx http-server -p 8081
````

[http://127.0.0.1:8081](http://127.0.0.1:8081) 

